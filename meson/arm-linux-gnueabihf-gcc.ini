[host_machine]
system = 'linux'
cpu_family = 'arm'
cpu = 'armv7'
endian = 'little'

[constants]
arch = 'arm-linux-gnueabihf'

[binaries]
c = arch + '-gcc'
cpp = arch + '-g++'
ld = arch + '-ld'
strip = arch + '-strip'
pkgconfig = arch + '-pkg-config'
exe_wrapper = [ 'qemu-arm', '-L', '/usr/arm-linux-gnueabihf' ]
